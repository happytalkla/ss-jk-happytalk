<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="skill">

	<!-- O2 app 화면이름 검색 -->
	<select id="selectO2appName" resultType="CMap">
		/* skill.selectO2appName */
		select * from shfda.tb_o2_app where screen_num = #{screenNum}
	</select>
</mapper>
